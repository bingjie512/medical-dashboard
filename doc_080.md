# 血氧与心电测试工具实验说明文档

**研发工程师：** 三杯橘  
**学号：** ***080  
**完成版本：** 进阶版

## 1. 使用的语言、框架与工具
本项目为完美适配 GitHub Pages 静态托管的需求，完全采用 Web 前端技术栈实现：
*   **开发语言**：TypeScript, HTML5, CSS3
*   **UI 框架**：React 18
*   **构建工具**：Vite (提供极速的模块热替换与生产打包)
*   **渲染工具**：HTML5 `<canvas>` (用于实现高性能的波形实时滚动渲染)
*   **图标库**：`lucide-react` (现代化精美图标)

## 2. 模块分离方式说明
项目基于高内聚、低耦合的架构理念，在 `src/core` 下分离出了如下独立的核心逻辑类，在需要时可被任意框架复用：
1.  **通信与数据源模块 (`playback_module_080.ts`)**：负责对 `ArrayBuffer` 进行按时间帧的切片，并提供进度控制（Play/Pause/Seek/Speed）。它向下解耦了具体数据从哪里来（可以是本地文件、`fetch` 获取的预置文件、甚至未来可通过 Web Serial 接收流）。
2.  **血氧解析模块 (`spo2_parser_080.ts`)**：实现了血氧协议的流式字节流解析与滑动窗口机制。不关心数据源和渲染层，仅通过回调暴露解析后的 `SpO2Data_080` 对象。
3.  **心电解析模块 (`ecg_parser_080.ts`)**：实现了 PCT 协议的心电数据解包（10字节带校验和验证），独立封装了校验逻辑及导联状态提取。
4.  **UI 渲染与波形显示 (`WaveformDisplay_080.tsx`, `App.tsx`)**：UI 组件层仅仅绑定了来自 Core 层的状态。波形部分抽离为独立的 Canvas React 组件，接受数值数组即可渲染平滑波形，实现了 View 和 Model 的完全分离。

## 3. 异常处理与防崩溃机制
*   **断点重连与错位恢复**：解析器在处理字节流时采用了逐字节滑动的状态机。例如在血氧解析中，若未检测到正确的包头（Bit 7 为 1 的字节）或者后续字节不符合要求（Bit 7 为 0），系统会自动丢弃最早的一个字节并继续尝试重新同步。即便存在大量的噪声数据，解析器也不会崩溃，且会在下一个正确的包到来时立即恢复。
*   **PCT 校验和保护**：心电模块严格遵守通信协议，只有 Checksum 校验成功的包才会被解包更新，这完全隔离了由文件损坏或信道噪声带来的脏数据影响。
*   **无效值与导联脱落处理**：解析器识别到导联脱落、探头脱落或处于寻找脉搏状态时，直接向下游发送 `null`。UI 层对 `null` 值进行了友好的 `---` 占位和标红高亮处理（例如显示"脱落报警"），避免将无效波形绘制在画布上或在 UI 上显示错误读数。
*   **渲染层容错**：Canvas 的波形显示对空数据进行了自我闭环处理，若长时间无数据进入缓冲，波形会自动走平，防止出现画面撕裂或卡死。
